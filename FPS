javascript:(function() {  var fpsContainer = document.createElement('div');  fpsContainer.style.position = 'fixed';  fpsContainer.style.top = '0';  fpsContainer.style.right = '0';  fpsContainer.style.background = 'black';  fpsContainer.style.color = 'white';  fpsContainer.style.padding = '5px';  fpsContainer.style.zIndex = '9999';  var fpsCounter = document.createElement('span');  fpsCounter.textContent = 'FPS: ';  fpsContainer.appendChild(fpsCounter);  var fpsValue = document.createElement('span');  fpsContainer.appendChild(fpsValue);  var frames = 0;  var startTime = performance.now();  function updateFPS() {    frames++;    var currentTime = performance.now();    var elapsedSeconds = (currentTime - startTime) / 1000;    if (elapsedSeconds >= 1) {      var fps = Math.round(frames / elapsedSeconds);      fpsValue.textContent = fps;      frames = 0;      startTime = currentTime;    }    requestAnimationFrame(updateFPS);  }  document.body.appendChild(fpsContainer);  updateFPS();})();
